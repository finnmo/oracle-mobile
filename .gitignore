node_modules/
dist/
.wrangler/
.env
.env.local
.dev.vars
*.local
wrangler.toml
wrangler.sandbox.toml
# Cloudflare Vite auto-setup stub — must not replace our Worker/D1/cron config
wrangler.json
wrangler.jsonc
.protect-databases
test.py
.venv/
# Private per-deploy backups (venues, branding) — do not publish
instances/*/
